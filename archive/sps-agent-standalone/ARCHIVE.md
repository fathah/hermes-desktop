# Archived SPS Agent Standalone App

Archived on 2026-06-25 during the SPS/main structural debt cleanup.

This Vite app is retained as reference material only. It is not a maintained build target and is not referenced by `package.json`, `electron-builder.yml`, or `electron.vite.config.ts`.

The canonical SPS Agent product surface lives at:

- `src/renderer/src/screens/SpsAgent/`

Do not make product changes here unless intentionally restoring or extracting this reference app.
