# Assistant provider: "mock" (default, fully offline) or "hermes"
VITE_ASSISTANT_PROVIDER=mock
# Hermes OpenAI-compatible API base (used by the /v1 dev proxy when provider=hermes)
VITE_HERMES_BASE_URL=http://localhost:8000
