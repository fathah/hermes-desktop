# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always be LF — CRLF breaks Linux shebang lines with
# "bad interpreter: /bin/bash\r: No such file or directory".
*.sh text eol=lf
