import{render,screen}from"@testing-library/react";import{describe,it,expect,vi}from"vitest";import PluginCenter from"./PluginCenter";describe("PluginCenter",()=>{it("shows manifest validation and no-code install gate",async()=>{Object.defineProperty(window,"hermesAPI",{configurable:true,value:{getHccPluginProposals:vi.fn().mockResolvedValue({items:[{id:"P1",pluginId:"plugin.focus",status:"pending_approval",manifest:{name:"Focus",version:"1.0.0",permissions:["context.read"],apps:[{id:"focus",title:"Focus"}]},validation:{valid:true,errors:[],appCount:1,permissionCount:1},security:{arbitraryCodeExecution:false,manifestOnly:true,approvalRequired:true}}]}),getHccPluginInstallations:vi.fn().mockResolvedValue({items:[]}),getHccPluginAudit:vi.fn().mockResolvedValue({items:[]}),approveHccPluginProposal:vi.fn(),installHccPluginProposal:vi.fn(),uninstallHccPlugin:vi.fn()}as unknown as typeof window.hermesAPI});render(<PluginCenter/>);expect(await screen.findByText(/Focus · 1.0.0/)).toBeInTheDocument();expect(screen.getByText(/arbitrary code false/)).toBeInTheDocument();expect(screen.getByText("Approve manifest")).toBeInTheDocument();expect(screen.queryByText("Install")).not.toBeInTheDocument()})});
