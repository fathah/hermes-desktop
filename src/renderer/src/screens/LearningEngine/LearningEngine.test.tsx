import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import LearningEngine from "./LearningEngine";
const base={hero:{title:"Learning Engine",subtitle:"Evidence"},items:[],recommendations:[],summary:{topics:0,synthesisDebt:0,applying:0,demonstrated:0},methodology:{mutationPolicy:"append_only_proposal_gated"}};
const deep={patterns:[{id:"learning-pattern.payload",signature:"missing-payload",category:"failure",projectIds:["A","B"],projectCount:2,evidenceCount:2,lessons:["Validate payload"],eligibleForPromotion:true}],promotions:[{id:"promo-1",patternId:"learning-pattern.payload",targetType:"skill_spec",targetId:"validate-payload",lesson:"Validate payload",status:"pending_approval"}],summary:{patterns:1,crossProject:1,pendingPromotions:1,appliedPromotions:0}};
describe("LearningEngine intelligence",()=>{beforeEach(()=>Object.defineProperty(window,"hermesAPI",{configurable:true,value:{getHccLearning:vi.fn().mockResolvedValue(base),getHccLearningIntelligence:vi.fn().mockResolvedValue(deep),createHccLearningTopic:vi.fn(),appendHccLearningEvent:vi.fn(),promoteHccLearningRecommendation:vi.fn(),stageHccLearningPromotion:vi.fn(),decideHccLearningPromotion:vi.fn()} as unknown as typeof window.hermesAPI}));
it("renders cross-project evidence and approval-gated promotions",async()=>{render(<LearningEngine/>);expect(await screen.findByText("Verified patterns and governed promotion")).toBeInTheDocument();expect(screen.getByText("2 projects · 2 evidence")).toBeInTheDocument();expect(screen.getByText("pending_approval · Validate payload")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Approve and apply"}));await waitFor(()=>expect(window.hermesAPI.decideHccLearningPromotion).toHaveBeenCalledWith("promo-1","approve","approve from native Learning Engine"));});});
