import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { describe,expect,it,vi } from "vitest";
import ContextInspectorRail from "./ContextInspectorRail";
const payload={schemaVersion:"context-inspector-v2",generatedAt:1,context:{entityType:"project",entityId:"P",title:"Private Project",status:"active"},tabs:{evidence:{status:"recorded",count:1,items:[{kind:"project_contract"}]},artifacts:{status:"not_recorded",count:0,items:[]},files:{status:"not_recorded",count:0,items:[]},memory:{status:"not_recorded",count:0,items:[]},skills:{status:"not_recorded",count:0,items:[]},logs:{status:"not_recorded",count:0,items:[]},approvals:{status:"not_recorded",count:0,items:[]}},provenance:{policy:"explicit_sources_only_policy_filtered",sourceRefs:[{type:"project",id:"P"}],readerScope:"tool",omissions:[{id:"mem-private",reason:"reader_policy_denied",sensitivity:"private"}]}};
describe("ContextInspectorRail policy",()=>{it("switches reader scope and surfaces policy omissions",async()=>{const fn=vi.fn().mockResolvedValue(payload);Object.defineProperty(window,"hermesAPI",{configurable:true,value:{getHccContextInspector:fn} as unknown as typeof window.hermesAPI});render(<ContextInspectorRail entityType="project" entityId="P" onClose={()=>{}}/>);expect(await screen.findByText("Private Project")).toBeInTheDocument();fireEvent.change(screen.getByLabelText("Inspector reader scope"),{target:{value:"tool"}});await waitFor(()=>expect(fn).toHaveBeenLastCalledWith("project","P","tool"));expect(screen.getByText("1 omitted by policy")).toBeInTheDocument();});});
